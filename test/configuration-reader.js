/*global describe, it*/
'use strict';

import 'babel-polyfill';
import ConfigurationReader from '../lib/configuration-reader';

describe('Configuration Reader', function () {
	it('Does the stuff', () => {
		console.log(new ConfigurationReader());
	});
});
